name: jeepneywaze_driver
description: JeepneyWaze — Driver app (opt-in GPS broadcast)
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  geolocator: ^12.0.0
  mqtt_client: ^10.3.0
  supabase_flutter: ^2.5.0
  dio: ^5.4.3+1
  go_router: ^13.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
